# Copy to .env and adjust for your environment.

# Comma-separated origins for CORS (default: http://localhost:5173)
# CORS_ORIGINS=http://localhost:5173

# Upstream stream read timeout in seconds (default: 300)
# STREAM_TIMEOUT_SECONDS=300

# UVICORN_HOST=0.0.0.0
# UVICORN_PORT=8000
